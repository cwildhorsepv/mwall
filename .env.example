echo "# Public API endpoint for the WaaS backend
NEXT_PUBLIC_WAAS_API_URL=http://localhost:4000/api/v1

# Branding toggle
NEXT_PUBLIC_BRAND_NAME=mwall
NEXT_PUBLIC_PRODUCT_NAME=\"Merlin Wallet\"" > .env.example

git add .env.example
git commit -m "Add .env.example with placeholders"
git push
